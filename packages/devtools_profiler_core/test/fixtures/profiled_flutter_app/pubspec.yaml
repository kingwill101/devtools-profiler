name: devtools_profiler_flutter_fixture
publish_to: none

environment:
  sdk: '>=3.13.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  devtools_region_profiler:
    path: ../../../../devtools_region_profiler
  marionette_flutter: ^0.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

dependency_overrides:
  devtools_profiler_protocol:
    path: ../../../../devtools_profiler_protocol

flutter:
  uses-material-design: true
