
name: devtools_profiler_cli
description: CLI and local stdio MCP server for automated Dart and Flutter CPU profiling.

version: 0.2.0

environment:
  sdk: '>=3.10.0 <4.0.0'

resolution: workspace

repository: https://github.com/kingwill101/devtools-profiler/tree/main/packages/devtools_profiler_cli

executables:
  devtools-profiler: devtools_profiler

dependencies:
  artisanal: ^0.3.0
  dart_mcp: ^0.5.0
  devtools_profiler_core: ^0.2.0
  path: ^1.9.0
  stream_channel: ^2.1.4
  vm_service: ^15.0.2

dev_dependencies:
  devtools_region_profiler: ^0.1.0
  test: ^1.25.8
